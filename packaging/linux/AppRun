#!/bin/sh
# AppImage entry point. Run from the AppDir's usr/bin so the game finds its
# assets (assets/) relative to the current directory.
HERE="$(dirname "$(readlink -f "$0")")"
cd "$HERE/usr/bin" || exit 1
exec "$HERE/usr/bin/snowboard_rush" "$@"
